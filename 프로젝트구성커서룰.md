인터랙티브 웹 디자인 프로젝트 구성 커서룰

📋 프로젝트 개요
이 가이드는 개발 초보 디자이너가 프롬프트만으로 인터랙티브 포트폴리오 사이트를 완성할 수 있도록 작성되었습니다.

> 전제조건: `@/data`, `@/components`, `@/assets` 폴더는 모두 준비 완료 상태

🎯 구현해야 할 핵심 요소

 라우터 구조 (실제 구현 대상)

/ (메인) → LandingPage.jsx
/project/:id → ProjectDetail.jsx


 LandingPage 섹션 구성 (실제 구현 대상)

TopSection.jsx      - 가로 스크롤 3단계 (Hero → Transition → Mission)
StorySection.jsx    - 브랜드 스토리 (스티키 배경)
ProjectsSection.jsx - 프로젝트 카드 그리드
ContactSection.jsx  - 연락처 및 강의 배너


 각 섹션별 사용 컴포넌트 리스트

TopSection 컴포넌트 구성:
- `FreeHorizontalScrollSection` - 가로 스크롤 컨테이너
- `HeroSection` - 첫 번째 화면
- `TransitionSection` - 두 번째 연결 화면  
- `MissionSection` - 세 번째 미션 화면

StorySection 컴포넌트 구성:
- `StickySection` - 스티키 스크롤 효과 컨테이너
- `Typography` (MUI) - 스토리 텍스트 표시
- `Stack` (MUI) - 레이아웃 배치

ProjectsSection 컴포넌트 구성:
- `ParallaxContainer` - 패럴랙스 스크롤 컨테이너
- `ParallaxLayer` - 다층 패럴랙스 레이어
- `ProjectCard` - 개별 프로젝트 카드
- `Grid` (MUI) - 반응형 그리드 레이아웃
- `getProjectsList()` - projectsData.js 데이터 함수

ContactSection 컴포넌트 구성:
- `FullPageSection` - 전체 화면 섹션 컨테이너
- `HumanIllustCarousel` - 인간 일러스트 캐러셀 템플릿
- `useIsInView` - 뷰포트 감지 훅
- `useBackground` - 배경색 관리 훅

 ProjectDetail 페이지 구성 (실제 구현 대상)

프로젝트 상세 정보 + 이미지 갤러리 + 이전/다음 버튼


 콘텐츠 데이터 시스템 (실제 구현 대상)

src/data/contentData.js - 모든 섹션의 텍스트 중앙 관리


섹션별 콘텐츠 데이터 활용:
- `heroContent` - "Design For\nOrdinary People" 메인 텍스트, 타이핑 설정
- `transitionContent` - "ORDINARY" 마스킹 텍스트
- `missionContent` - "Ordinary Mission" 제목, 브랜드 미션 메시지
- `storyContent` - 김도연 베이커리 Bloom 스토리, 인용구
- `projectsContent` - "Ordinary Projects" 섹션 제목
- `contactContent` - "Let's Work Together" 제목, 연락처 정보
- `siteMetadata` - "Ordinary Design" 브랜드명, 네비게이션 메뉴

🌈 특별한 배경 시스템 이해하기

이 사이트의 가장 독특한 특징은 `BackgroundLayer.jsx`를 통한 전역 배경 관리입니다.

 배경 작동 원리
1. 고정 배경 레이어: 화면 전체를 덮는 고정된 배경판
2. 섹션별 색상 전환: 스크롤 시 배경색이 부드럽게 변화
3. 컨텍스트 기반 관리: `BackgroundContext`가 현재 색상 상태 관리
4. 커서 연동: 배경 테마에 따라 커서 색상도 자동 변경

 프롬프트에서 이렇게 표현하세요
- ❌ "배경색을 설정하세요" 
- ✅ "스크롤할 때 화면 전체 배경이 [색상]으로 부드럽게 전환되어야 합니다"

🎨 스크롤 & 애니메이션 시스템

 스크롤 엔진
- Lenis: 부드러운 스크롤 담당
- GSAP ScrollTrigger: 스크롤 기반 애니메이션
- 자동 스크롤 위치 복원: 페이지 이동 후 돌아왔을 때 원래 위치로

 프롬프트에서 이렇게 표현하세요
- ❌ "GSAP로 애니메이션 만들어주세요"
- ✅ "스크롤하면서 요소들이 아래에서 위로 나타나는 효과를 만들어주세요"

🎯 프롬프트 작성 가이드 (디자이너 관점)

 레이아웃 표현법

"전체 화면 높이의 섹션" → TopSection
"가로로 스크롤되는 영역" → HorizontalScrollSection  
"카드 그리드 형태" → ProjectsSection
"고정된 상단 메뉴" → Header


 인터랙션 표현법

"마우스 커서가 O 모양에서 D로 변하는" → CustomTooltipCursor
"호버하면 살짝 커지는 효과" → 카드 hover 애니메이션
"스크롤하면 순서대로 나타나는" → stagger 애니메이션
"클릭하면 해당 섹션으로 이동" → 섹션 네비게이션


 배경 시스템 표현법

"스크롤 시 배경이 흰색에서 검은색으로" → BackgroundContext 색상 전환
"섹션마다 다른 배경색" → 섹션별 배경 설정
"부드러운 색상 변화" → 배경 트랜지션


🔄 상태 관리 시스템 이해

 SectionRefsContext
- 각 섹션의 위치 참조 관리
- 네비게이션 클릭 시 해당 섹션으로 스크롤

 BackgroundContext  
- 현재 배경색 상태 관리
- 스크롤 위치에 따른 배경색 자동 전환
- 커서 색상도 연동

🎨 실제 구현될 데이터 활용

 projectsData.js 활용
- 프로젝트 목록 자동 렌더링
- 썸네일, 제목, 설명 매핑
- 상세 페이지 자동 생성

 contentData.js 활용  
- 모든 섹션의 텍스트 중앙 관리
- 다국어 지원 및 콘텐츠 수정 용이성
- UI와 데이터 분리로 유지보수성 향상

 프롬프트에서 이렇게 표현하세요
- ❌ "projectsData에서 map으로..."
- ✅ "프로젝트 데이터에 있는 모든 프로젝트들이 카드 형태로 보여져야 합니다"
- ❌ "텍스트를 하드코딩으로..."
- ✅ "콘텐츠 데이터에서 가져온 텍스트로 표시해주세요" 