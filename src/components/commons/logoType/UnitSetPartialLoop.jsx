import React from 'react';
import { LogoTypeUnitPartialLoop } from "./LogoTypeUnitPartialLoop";
import {
	path_D_reverse,
	path_a,
	path_e,
	path_i,
	path_g,
	path_n,
	path_s,
	path_B_geometric,
	path_L,
} from "../../../data/logoTypePathData";

export const D_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_D_reverse}
			strokeWidth={72}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const E_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_e}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const S_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_s}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const I_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_i}
			width={48}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const G_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_g}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const N_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_n}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const L_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_L}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const A_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_a}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};

export const B_PartialLoop = ({
	startDelay = 0,
	delay = 0,
	scale = 1,
	pathClassName,
	color,
	loopDuration = 2000,
	visiblePercent = 80,
	ease,
	isTrigger = true,
}) => {
	return (
		<LogoTypeUnitPartialLoop
			data={path_B_geometric}
			strokeWidth={48}
			delay={delay}
			startDelay={startDelay}
			scale={scale}
			pathClassName={pathClassName}
			color={color}
			isTrigger={isTrigger}
			loopDuration={loopDuration}
			visiblePercent={visiblePercent}
			ease={ease}
		/>
	);
};
